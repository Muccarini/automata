{
  "name": "automata",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @automata/frontend",
    "dev:backend": "npm run dev --workspace @automata/backend",
    "lint": "npm run lint --workspace @automata/frontend && npm run lint --workspace @automata/backend",
    "build": "npm run build --workspace @automata/shared-contracts && npm run build --workspace @automata/frontend && npm run build --workspace @automata/backend",
    "preview": "npm run preview --workspace @automata/frontend",
    "db:migrate": "npm run db:migrate --workspace @automata/backend",
    "db:seed": "npm run db:seed --workspace @automata/backend"
  }
}
